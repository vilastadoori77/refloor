{
  "name": "inventory-service",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Inventory Service — consolidation, snapshot cache, refresh worker, health (SPEC 02). Port 4100.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@inventory/shared": "*",
    "cors": "^2.8.5",
    "express": "^4.21.2"
  },
  "optionalDependencies": {
    "ioredis": "^5.4.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.2"
  }
}
